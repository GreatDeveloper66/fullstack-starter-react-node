{
  "name": "fullstack-starter-react-node",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "proxy": "http://localhost:5000",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && cd frontend && npm install && npm run build && cd..",
    "heroku-postbuild": "npm run build",
    "dev": "concurrently \"cd frontend && npm start\" \"nodemon server.js\"",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "react-router-dom": "^7.9.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
